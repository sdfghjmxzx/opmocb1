"""Effects application bridge skeleton.
Phase 1: no-op; populated in later phases.
"""
class EffectsBridge:
    def apply(self, effect_block: dict, context: dict):
        # Placeholder; real application in later phases.
        return context
